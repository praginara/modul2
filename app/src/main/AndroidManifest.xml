<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.randypraginara.randypraginara_1106130167_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".tampilanawal"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pilihmenu"/>
        <activity android:name=".takeaway"/>
        <activity android:name=".DineIn"/>
        <activity android:name=".Menu"/>
        <activity android:name=".DetailMenu" android:parentActivityName=".Menu">
        <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".DetailMenu"/>
        </activity>
    </application>

</manifest>